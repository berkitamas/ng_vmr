import { LineMissedDirective } from './line-missed.directive';

describe('LineMissedDirective', () => {
  it('should create an instance', () => {
    const directive = new LineMissedDirective();
    expect(directive).toBeTruthy();
  });
});
